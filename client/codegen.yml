overwrite: true
schema: "https://mindfriend.onrender.com/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/graphql/generated/sdk.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
    config:
      rawRequest: true
